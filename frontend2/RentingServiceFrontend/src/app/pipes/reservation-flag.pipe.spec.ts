import { ReservationFlagPipe } from './reservation-flag.pipe';

describe('ReservationFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
